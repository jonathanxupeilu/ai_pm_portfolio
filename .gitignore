# Python
__pycache__/
*.py[cod]
.venv/
venv/

# ---- 真实数据：含真实公司名与岗位信息，绝不入库 ----
ledger.csv
pool/
shortlists/

# ---- 令牌与凭据 ----
# 本流程只从 ~/.workbuddy/mcp.json 读令牌，项目内不应出现任何凭据；
# 下面几条是兜底，防止以后误加配置文件时把密钥带进版本库。
secrets.json
.env
*.local.*
