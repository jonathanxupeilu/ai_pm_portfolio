# 打分标准

> 这是**打分口径的唯一真源**，由你维护。`scripts/score.py` 每次运行都读它。
> 改完直接生效，不需要动任何代码。

## 原理

`score.py` 会把 JD 正文与下表逐个关键词做**忽略大小写的子串匹配**，然后算：

```
匹配度 = 命中关键词的权重和 ÷ 全部关键词的权重和 × 100
```

命中哪些词会一并记进 `pool/jobs.csv` 的 `hits` 列，所以**分数永远可解释**——
一个岗位排前面，你能直接看出是命中了哪几个词。

**注意**：匹配不看语义。`大模型` 和 `LLM` 在它眼里是两个无关的词，`Agent` 和 `智能体`
同理。所以同义词请在下面**各写一行**。权重给 3 的词就是比权重 1 的词重要。

## 关键词

| 关键词 | 权重 |
|---|---|
| AI产品 | 3 |
| 大模型 | 3 |
| Agent | 3 |
| LLM | 2 |
| RAG | 2 |
| 智能体 | 2 |
| 提示词 | 1 |
| Prompt | 1 |
| 知识库 | 1 |
| 评测 | 1 |
| 数据回流 | 1 |
| 产品规划 | 2 |
| 需求分析 | 2 |
| 商业化 | 1 |
| ToB | 1 |
| SaaS | 1 |

## 怎么改

- **加词**：在上面表格里加一行 `| 词 | 权重 |`，权重写 1–5，不写默认为 1。
- **删词**：删掉那一行。
- **调重要性**：改权重数字。
- 表格只要有 `| 关键词 | 权重 |` 这个表头，下面每行都会被读；`#` 注释行和空行会被跳过。

## 如果以后想换成语义匹配

`config.json` 里的 `matcher` 改成 `embedding` 即可，但需要先在
`scripts/matcher.py` 里实现 `EmbeddingMatcher`（当前它是空位，调用会明确报错而不是悄悄算错分）。
可选方案：`model2vec` 或 `fastembed`，纯 CPU、不需要 torch。
